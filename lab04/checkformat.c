#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int main(int argc, char* argv[]){

    if(argc!=3){
        printf("Invalid Argument Counts.\n Required 3 Arguments but only found %i.\nUsage: ./checkformat <filename>.dat\n", argc);
        exit(EXIT_FAILURE);
    }

    FILE* fp;
    

    ret = fscanf(fp, "%d", *n);
    if(ret==1){
        ret = fscanf(fp,%d,%d);
    }



    exit(EXIT_SUCCESS);
}